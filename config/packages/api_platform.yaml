api_platform:
    mapping:
        paths: ['%kernel.project_dir%/src/Entity']
    patch_formats:
        json: ['application/merge-patch+json']
    swagger:
        versions: [3]
    # exemple de suppression de pagination
    # collection:
    #     pagination:
    #         enabled: false
    #         items_per_page: 10
